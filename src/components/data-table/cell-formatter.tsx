import React from 'react';
import capitalize from 'lodash/capitalize';
// import day from 'dayjs';
// import { formatTimeRangeDisplay } from 'shared/utils/tools';
import get from 'lodash/get';

// export const capitalizeCell = (key: string) => (row: { [key: string]: string }) => <>{capitalize(row[key])}</>;

// export const formatDate = (key: string, format: string) => (row: { [key: string]: string }) =>
//   <>{day(row[key]).format(format)}</>;

// export const timeRange = (start_key: string, end_key: string) => (row: { [key: string]: string }) =>
//   <>{formatTimeRangeDisplay(start_key, end_key, row)}</>;

// export const listColumn = (key: string) => (row: { [key: string]: string[] }) => {
//   const col = get(row, key, []);
//   return <>{col ? col.join(', ') : ''}</>;
// };
